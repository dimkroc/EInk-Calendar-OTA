# EInk-Calendar-OTA

Public distribution channel for signed EInk Calendar firmware updates.

This repository intentionally contains generated OTA artifacts only:

- `firmware.bin`
- `latest.json`
- `latest.sig`

Source code remains in the private `dimkroc/EInk-Calendar` repository.
